/** Starting watchlist. The user's edits are kept in localStorage on top of this. */
export const defaultWatchlist = ["SPY", "QQQ", "AAPL", "MSFT", "NVDA", "AMZN", "GOOGL", "TSLA"];

/** The benchmark every screen compares against. */
export const benchmarkSymbol = "SPY";
